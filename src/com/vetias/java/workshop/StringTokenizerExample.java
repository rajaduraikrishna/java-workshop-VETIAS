package com.vetias.java.workshop;
public class StringTokenizerExample {

    public static void main(String[] args) {
        // String text = "This program shows how can we split a string into multiple strings. We need to find sentance, word and letter count";
        // StringTokenizer tokenizer = new StringTokenizer(text, ".");
        // System.out.println("Number of sentences: " + tokenizer.countTokens());  
        // StringTokenizer wordTokenizer = new StringTokenizer(text, " ");
        // System.out.println("Number of words: " + wordTokenizer.countTokens());
        // while(wordTokenizer.hasMoreTokens()) {
        //     // Get the next word from the tokenizer
        //     // and print it
        //     System.out.println(wordTokenizer.nextToken());
        // }
        System.out.println("\"Java\" is fun");
    }
}
